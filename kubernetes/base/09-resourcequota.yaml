apiVersion: v1
kind: ResourceQuota
metadata:
  name: infraops-quota
  namespace: infraops
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "3"
    limits.memory: 3Gi
    pods: "20"
    persistentvolumeclaims: "4"
